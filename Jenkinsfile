pipeline {
  agent any
     stages {
       stage ("build") {
         steps {
           echo "build stage is in progress"
         }
       }
       stage ("test") {
         steps {
           echo "test stage is in progress"
         }         
       }
       stage ("deploy") {
         steps {
           echo "deploy stage is in progress"
         }         
       }       
     }
}
